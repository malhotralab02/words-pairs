import { DisableOnEnterDirectiveDirective } from './disable-on-enter-directive.directive';

describe('DisableOnEnterDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new DisableOnEnterDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
