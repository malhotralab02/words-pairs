import { DisableOnEnterDirective } from './disable-on-enter.directive';

describe('DisableOnEnterDirective', () => {
  it('should create an instance', () => {
    const directive = new DisableOnEnterDirective();
    expect(directive).toBeTruthy();
  });
});
